import { ProviderIcon, ProviderAvatar, ModelIcon, ModelAvatar, ModelTypeIcon } from "yunui/ai";

一組用於 AI 提供商與模型的圖示元件。圖示會根據 id 從內建的 `/icons` 資源中解析，並帶有合理的回退。`ProviderIcon`/`ModelIcon` 預設為扁平樣式；`*Avatar` 預設一律為圓角圖塊。`ModelIcon` 按 模型圖示 → 開發者圖示 → 提供商圖示 的優先順序進行解析。

## 匯入

```tsx
import {
  ProviderIcon,
  ProviderAvatar,
  ModelIcon,
  ModelAvatar,
  ModelTypeIcon,
} from "@yuhuanowo/yunui/ai";
```

## 供應商圖示

傳入 `provider` id（例如 `openai`、`anthropic`、`google`、`deepseek`、`mistral`）。設定 `rounded` 可得到圓角圖塊，或直接使用預設圓角的 `ProviderAvatar`。

<ComponentPreview
  title="供應商圖示"
  code={`<ProviderIcon provider="openai" size={32} />
<ProviderIcon provider="anthropic" size={32} />
<ProviderIcon provider="google" size={32} rounded />
<ProviderAvatar provider="deepseek" size={32} />
<ProviderAvatar provider="mistral" size={32} />`}
>
  <div className="flex items-center gap-4">
    <ProviderIcon provider="openai" size={32} />
    <ProviderIcon provider="anthropic" size={32} />
    <ProviderIcon provider="google" size={32} rounded />
    <ProviderAvatar provider="deepseek" size={32} />
    <ProviderAvatar provider="mistral" size={32} />
  </div>
</ComponentPreview>

## 模型圖示

`ModelIcon` 會按 模型 → 開發者 → 提供商 的順序回退。此處的 `developer` id 解析出專屬的模型家族圖示。`ModelAvatar` 是圓角預設。

<ComponentPreview
  title="模型圖示"
  code={`<ModelIcon provider="anthropic" developer="claude" size={32} />
<ModelAvatar provider="google" developer="gemini" size={32} />
<ModelAvatar provider="deepseek" developer="deepseek" size={32} />`}
>
  <div className="flex items-center gap-4">
    <ModelIcon provider="anthropic" developer="claude" size={32} />
    <ModelAvatar provider="google" developer="gemini" size={32} />
    <ModelAvatar provider="deepseek" developer="deepseek" size={32} />
  </div>
</ComponentPreview>

## 模型類型圖示

`ModelTypeIcon` 為模型的模態（`chat`、`embedding`、`image_generation`、`tts`、`video` 等）算繪帶色字形，未匹配時回退到通用的機器人圖示。

<ComponentPreview
  title="模型類型圖示"
  code={`<ModelTypeIcon type="chat" size={20} />
<ModelTypeIcon type="embedding" size={20} />
<ModelTypeIcon type="image_generation" size={20} />
<ModelTypeIcon type="tts" size={20} />
<ModelTypeIcon type="video" size={20} />`}
>
  <div className="flex items-center gap-4">
    <ModelTypeIcon type="chat" size={20} />
    <ModelTypeIcon type="embedding" size={20} />
    <ModelTypeIcon type="image_generation" size={20} />
    <ModelTypeIcon type="tts" size={20} />
    <ModelTypeIcon type="video" size={20} />
  </div>
</ComponentPreview>

## ProviderIcon 屬性

<PropsTable name="ProviderIcon" />

## ProviderAvatar 屬性

<PropsTable name="ProviderAvatar" />

## ModelIcon 屬性

<PropsTable name="ModelIcon" />

## ModelAvatar 屬性

<PropsTable name="ModelAvatar" />

## ModelTypeIcon 屬性

<PropsTable name="ModelTypeIcon" />
