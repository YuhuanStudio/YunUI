import { ModelCard, ProviderIcon } from "yunui/ai";

用於概覽某個 AI 模型的卡片：提供商/模型圖示、顯示名稱、能力標籤、可複製的 id 徽章、簡短描述，以及包含開發者、上下文視窗、等級與價格的頁尾。傳入 `onClick` 可讓整張卡片成為可用鍵盤啟用的按鈕。

## Import

```tsx
import { ModelCard } from "@yuhuanowo/yunui/ai";
```

## Basic

`icon` 插槽由宿主渲染——傳入 `<ProviderIcon>`（或 `<ModelIcon>`），讓卡片保持與框架無關。

<ComponentPreview
  title="Basic"
  code={`import { ModelCard, ProviderIcon } from "@yuhuanowo/yunui/ai";

<ModelCard
  name="GPT-4o"
  icon={<ProviderIcon provider="openai" size={32} rounded />}
  ids={["gpt-4o"]}
  description="Multimodal flagship model with vision and fast responses."
  capabilities={["vision", "streaming", "function_calling"]}
  developer={{ label: "OpenAI" }}
  context="128K"
  price="$2.50/M"
/>`}
>
  <div className="w-full max-w-xs">
    <ModelCard
      name="GPT-4o"
      icon={<ProviderIcon provider="openai" size={32} rounded />}
      ids={["gpt-4o"]}
      description="Multimodal flagship model with vision and fast responses."
      capabilities={["vision", "streaming", "function_calling"]}
      developer={{ label: "OpenAI" }}
      context="128K"
      price="$2.50/M"
    />
  </div>
</ComponentPreview>

## Status badges

使用 `nonofficial` 與 `suspended` 標記模型的可用狀態。除非 `tier` 為 `"free"`，否則都會顯示等級標籤。

<ComponentPreview
  title="Status badges"
  code={`<ModelCard
  name="Claude Opus 4"
  icon={<ProviderIcon provider="anthropic" size={32} rounded />}
  ids={["claude-opus-4", "claude-opus-4-latest"]}
  description="High-reasoning model for complex, long-running tasks."
  capabilities={["thinking", "vision", "function_calling"]}
  developer={{ label: "Anthropic" }}
  context="200K"
  tier="pro"
  nonofficial
/>`}
>
  <div className="w-full max-w-xs">
    <ModelCard
      name="Claude Opus 4"
      icon={<ProviderIcon provider="anthropic" size={32} rounded />}
      ids={["claude-opus-4", "claude-opus-4-latest"]}
      description="High-reasoning model for complex, long-running tasks."
      capabilities={["thinking", "vision", "function_calling"]}
      developer={{ label: "Anthropic" }}
      context="200K"
      tier="pro"
      nonofficial
    />
  </div>
</ComponentPreview>

## Props

<PropsTable name="ModelCard" />
