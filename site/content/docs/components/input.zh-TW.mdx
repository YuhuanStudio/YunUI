import { Input } from "yunui";
import { Search, Mail } from "lucide-react";

單行文字輸入框。它呈現標準的輸入框樣式，支援可選的前置 `icon`，並顯示內聯的 `error` 錯誤訊息（同時會將邊框切換為錯誤樣式）。所有原生 `<input>` 屬性都會透傳。

## 匯入

```tsx
import { Input } from "@yuhuanowo/yunui";
```

## 基礎用法

<ComponentPreview
  title="基礎用法"
  code={`<Input placeholder="you@example.com" />`}
>
  <div className="w-full max-w-xs">
    <Input placeholder="you@example.com" />
  </div>
</ComponentPreview>

## 帶圖示

<ComponentPreview
  title="帶前置圖示"
  code={`import { Search } from "lucide-react";

<Input icon={<Search className="w-4 h-4" />} placeholder="Search…" />`}
>
  <div className="w-full max-w-xs">
    <Input icon={<Search className="w-4 h-4" />} placeholder="Search…" />
  </div>
</ComponentPreview>

## 錯誤狀態

<ComponentPreview
  title="帶錯誤訊息"
  code={`<Input
  icon={<Mail className="w-4 h-4" />}
  defaultValue="not-an-email"
  error="Enter a valid email address."
/>`}
>
  <div className="w-full max-w-xs">
    <Input
      icon={<Mail className="w-4 h-4" />}
      defaultValue="not-an-email"
      error="Enter a valid email address."
    />
  </div>
</ComponentPreview>

## 停用

<ComponentPreview
  title="停用"
  code={`<Input placeholder="Disabled" disabled />`}
>
  <div className="w-full max-w-xs">
    <Input placeholder="Disabled" disabled />
  </div>
</ComponentPreview>

## 屬性

<PropsTable name="Input" />
