import { Checkbox } from "yunui";

受控的核取方塊。它僅支援受控模式：傳入 `checked` 和 `onCheckedChange`。將它與 `<label htmlFor>`（透過 `id` prop）配對，可構成一個無障礙的、點擊即可切換的列。

## 匯入

```tsx
import { Checkbox } from "@yuhuanowo/yunui";
```

## 用法

`Checkbox` 是受控元件——將它與 state 關聯：

```tsx
const [checked, setChecked] = useState(false);
<label className="flex items-center gap-2">
  <Checkbox checked={checked} onCheckedChange={setChecked} id="terms" />
  Accept terms
</label>
```

## 範例

<ComponentPreview title="核取方塊列">
  <CheckboxDemo />
</ComponentPreview>

## 屬性

<PropsTable name="Checkbox" />
