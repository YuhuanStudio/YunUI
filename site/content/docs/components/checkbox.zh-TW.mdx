import { Checkbox } from "yunui";

受控的核取方塊。它僅支援受控模式：傳入 `checked` 和 `onCheckedChange`。將它與 `<label htmlFor>`（透過 `id` prop）配對，可構成一個無障礙的、點擊即可切換的列。

## 匯入

```tsx
import { Checkbox } from "@yuhuanowo/yunui";
```

## 用法

`Checkbox` 是受控元件——將它與 state 關聯：

```tsx
const [checked, setChecked] = useState(false);
<label className="flex items-center gap-2">
  <Checkbox checked={checked} onCheckedChange={setChecked} id="terms" aria-label="Accept terms" />
  Accept terms
</label>
```

## 範例

<ComponentPreview title="核取方塊列">
  <CheckboxDemo />
</ComponentPreview>

## 如何命名

`Checkbox` 渲染的是 `<button role="checkbox">`。`<label>`——不論是包住它還是用 `htmlFor`——**都無法命名它**：label 只對真正的表單控制項有效。請用 `aria-label`，或讓 `aria-labelledby` 指向旁邊的文字。

```tsx
<span className="flex items-center gap-2 text-sm">
  <Checkbox checked={checked} onCheckedChange={setChecked} aria-labelledby="terms-label" />
  <span id="terms-label">I accept the terms</span>
</span>
```

## 屬性

<PropsTable name="Checkbox" />
