import { PasswordInput } from "yunui";

内建显示／隐藏切换的密码输入框。

## 导入

```tsx
import { PasswordInput } from "@yuhuanowo/yunui";
```

## 用法

它转发全部 `<input>` 属性，因此可以直接替换表单里原本的 `<Input type="password">`——受控或非受控皆可，`name`、`autoComplete`、`required` 等一应俱全。

<ComponentPreview title="Password input">
  <div className="w-full max-w-sm flex flex-col gap-4">
    <PasswordInput placeholder="Enter your password" defaultValue="correct-horse" />
    <PasswordInput placeholder="Confirm password" defaultValue="wrong" error="Passwords do not match." />
    <PasswordInput placeholder="Disabled" disabled />
  </div>
</ComponentPreview>

## 说明

- 切换按钮是输入框的 flex 同级元素，**不是**绝对定位的浮层。Safari 会把绝对定位的版本摆到字段外面；flex 行则在各处渲染一致。
- 切换按钮为 `tabIndex={-1}`，因此在登录表单中按 Tab 是密码 → 提交，不会绕道一个只改变可见性的按钮。
- 它带有 `aria-pressed`，标签会在显示／隐藏之间切换——可用 `labels={{ show, hide }}` 覆盖英文默认值。
- `error` 会设置 `aria-invalid` 并以 `aria-describedby` 关联讯息。

## 属性

<PropsTable name="PasswordInput" />
