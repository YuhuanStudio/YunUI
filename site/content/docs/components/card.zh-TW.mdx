import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter, Badge, Button } from "yunui";

渲染標準 `.card` 樣式的表面容器——與原始 `className="card"` 用法外觀一致，但收斂為一個元件。傳入 `hover` 可獲得互動式抬升效果。

## 匯入

```tsx
import { Card } from "@yuhuanowo/yunui";
```

## 基本用法

<ComponentPreview
  title="Card"
  code={`<Card className="p-5 w-60">
  <p className="font-medium mb-1">Pro plan</p>
  <p className="text-caption">Everything in Basic, plus higher limits.</p>
</Card>`}
>
  <Card className="p-5 w-60">
    <p className="font-medium mb-1">Pro plan</p>
    <p className="text-caption">Everything in Basic, plus higher limits.</p>
  </Card>
</ComponentPreview>

## 懸停

`hover` 為可點擊的卡片新增邊框 + 陰影過渡效果。

<ComponentPreview
  title="懸停上浮"
  code={`<Card hover className="p-5 w-60">
  <div className="flex items-center justify-between mb-2">
    <span className="font-medium">Pro plan</span>
    <Badge variant="success">Active</Badge>
  </div>
  <p className="text-caption">Hover me — the border and shadow animate.</p>
</Card>`}
>
  <Card hover className="p-5 w-60">
    <div className="flex items-center justify-between mb-2">
      <span className="font-medium">Pro plan</span>
      <Badge variant="success">Active</Badge>
    </div>
    <p className="text-caption">Hover me — the border and shadow animate.</p>
  </Card>
</ComponentPreview>

## 組合

`Card` 是一個不帶預設的容器——可以在其中組合任意內容，包括其他基礎元件。

<ComponentPreview title="帶操作">
  <Card className="p-5 w-72">
    <p className="font-medium mb-1">Upgrade your plan</p>
    <p className="text-caption mb-4">Unlock higher rate limits and priority support.</p>
    <Button variant="primary" size="sm">Upgrade</Button>
  </Card>
</ComponentPreview>

## 組成

`Card` 單獨用就夠了，但它同時提供下面五個插槽——當多張卡片需要把標題與操作對齊時用它們，而不是每張卡各自發明一套內距。

| 部件 | 作用 |
| --- | --- |
| `Card` | 表面本身（邊框、圓角、背景、可選 `hover`）。 |
| `CardHeader` | 標頭區域，`p-6`，子元素以 `space-y-1.5` 堆疊。 |
| `CardTitle` | 標題——渲染為真正的 `<h3>`，讓頁面保有文件大綱。 |
| `CardDescription` | 標題下方的淡出說明行。 |
| `CardContent` | 主體區域；`p-6 pt-0`，緊貼標頭下方。 |
| `CardFooter` | 操作／附註列；`p-6 pt-0`，flex 且垂直置中。 |

每個部件都會轉發 ref 與全部標準 `<div>`（或 `<h3>`／`<p>`）屬性。

<ComponentPreview title="組成">
  <Card className="w-80">
    <CardHeader>
      <CardTitle>Monthly usage</CardTitle>
      <CardDescription>Resets on the 1st.</CardDescription>
    </CardHeader>
    <CardContent>
      <p className="text-2xl font-semibold tabular-nums">2,481,930</p>
      <p className="text-caption">tokens of 5,000,000</p>
    </CardContent>
    <CardFooter>
      <Button variant="secondary" size="sm">View breakdown</Button>
    </CardFooter>
  </Card>
</ComponentPreview>

## 屬性

<PropsTable name="Card" />

`Card` 還會轉發每一個標準 `<div>` 屬性（`className`、`onClick`、`style` 等）。
