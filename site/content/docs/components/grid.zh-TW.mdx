import { Grid, Card } from "yunui";

一個 CSS grid 佈局基礎元件。`columns`、`rows`、`gap`、`align` 和 `padding` 會透過靜態對應表對應到字面的 Tailwind 類別（因此編譯器總會產生它們）。純展示型；`as` 可改變渲染的標籤。

## 匯入

```tsx
import { Grid } from "@yuhuanowo/yunui";
```

## 欄

<ComponentPreview
  title="3 columns"
  code={`<Grid columns={3} gap={3}>
  <Box>1</Box><Box>2</Box><Box>3</Box>
  <Box>4</Box><Box>5</Box><Box>6</Box>
</Grid>`}
>
  <Grid columns={3} gap={3} className="w-full max-w-md">
    {[1, 2, 3, 4, 5, 6].map((n) => (
      <Card key={n} className="py-4 grid place-items-center font-mono">{n}</Card>
    ))}
  </Grid>
</ComponentPreview>

## 間距

<ComponentPreview
  title="2 columns, wider gap"
  code={`<Grid columns={2} gap={6}>
  <Box>A</Box><Box>B</Box>
</Grid>`}
>
  <Grid columns={2} gap={6} className="w-full max-w-sm">
    <Card className="py-6 grid place-items-center font-mono">A</Card>
    <Card className="py-6 grid place-items-center font-mono">B</Card>
  </Grid>
</ComponentPreview>

## 響應式

`Grid` 設定固定的欄數；如需響應式佈局，可透過 `className` 結合 Tailwind 的響應式工具類別，或改用原始的 `grid-cols-*` 類別。

<ComponentPreview
  title="Stat tiles"
  code={`<Grid columns={4} gap={4}>
  {stats.map((s) => <StatTile key={s} />)}
</Grid>`}
>
  <Grid columns={4} gap={4} className="w-full max-w-2xl">
    {["1.2k", "48k", "3", "72.4"].map((v, i) => (
      <Card key={i} className="p-4">
        <div className="text-label mb-1">metric</div>
        <div className="text-2xl font-semibold">{v}</div>
      </Card>
    ))}
  </Grid>
</ComponentPreview>

## 屬性

<PropsTable name="Grid" />
