import { CodeDemo } from "yunui/patterns";

一个开箱即用的多标签代码示例，展示如何用 Python、Node.js 和 cURL 调用兼容 OpenAI 的 API。它是基于 `CodeBlock` 构建的自包含展示块（支持标签切换和一键复制）——适合用在 API 落地页或快速上手指南。

## 引入

```tsx
import { CodeDemo } from "@yuhuanowo/yunui/patterns";
```

## 示例

代码片段本身已内置，但片段里属于**你的服务**的每个值都是属性。默认值指向
`https://api.example.com/v1`，因此还没配置前也能正常呈现。

<ComponentPreview title="CodeDemo">
  <CodeDemo />
</ComponentPreview>

### 指向自己的网关

<ComponentPreview title="自定义网关">
  <CodeDemo
    baseUrl="https://api.acme.dev/v1"
    model="acme-large"
    apiKeyPlaceholder="sk-..."
    prompt="Summarise this."
  />
</ComponentPreview>

## 属性

<PropsTable name="CodeDemo" />
