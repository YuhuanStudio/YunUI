import { PageLoadingState, PageErrorState, PageEmptyState } from "yunui/patterns";
import { Button } from "yunui";

三个居中、占满区块的占位组件，对应常见的页面生命周期：加载中、错误和空状态。它们共享一致的间距与排版，因此页面在数据加载过程中可以干净地在它们之间切换。

## Import

```tsx
import { PageLoadingState, PageErrorState, PageEmptyState } from "@yuhuanowo/yunui/patterns";
```

## PageLoadingState

带可选 `message` 的居中加载指示器。

<ComponentPreview
  title="PageLoadingState"
  code={`<PageLoadingState message="Loading your data…" />`}
>
  <div className="w-full max-w-xl">
    <PageLoadingState message="Loading your data…" />
  </div>
</ComponentPreview>

## PageErrorState

带可选重试链接的居中错误 `message`（当提供 `onRetry` 时显示）。

<ComponentPreview
  title="PageErrorState"
  code={`<PageErrorState message="Failed to load data." />`}
>
  <div className="w-full max-w-xl">
    <PageErrorState message="Failed to load data." />
  </div>
</ComponentPreview>

## PageEmptyState

带 `title`、可选 `description`、可选前置 `icon` 组件以及用于放置按钮的 `action` 插槽的居中空状态。

<ComponentPreview
  title="PageEmptyState"
  code={`<PageEmptyState
  title="No projects yet"
  description="Create your first project to get started."
  action={<Button variant="primary">New project</Button>}
/>`}
>
  <div className="w-full max-w-xl">
    <PageEmptyState
      title="No projects yet"
      description="Create your first project to get started."
      action={<Button variant="primary">New project</Button>}
    />
  </div>
</ComponentPreview>

## Props

<PropsTable name="PageLoadingState" />

<PropsTable name="PageErrorState" />

<PropsTable name="PageEmptyState" />
