import { CodeDemo } from "yunui/patterns";

一個開箱即用的多標籤程式碼範例，展示如何用 Python、Node.js 和 cURL 呼叫相容 OpenAI 的 API。它是基於 `CodeBlock` 建構的自包含展示區塊（支援標籤切換和一鍵複製）——適合用在 API 著陸頁或快速上手指南。

## 匯入

```tsx
import { CodeDemo } from "@yuhuanowo/yunui/patterns";
```

## 範例

程式碼片段本身已內建，但片段裡屬於**你的服務**的每個值都是屬性。預設值指向
`https://api.example.com/v1`，因此還沒設定前也能正常呈現。

<ComponentPreview title="CodeDemo">
  <CodeDemo />
</ComponentPreview>

### 指向自己的閘道

<ComponentPreview title="自訂閘道">
  <CodeDemo
    baseUrl="https://api.acme.dev/v1"
    model="acme-large"
    apiKeyPlaceholder="sk-..."
    prompt="Summarise this."
  />
</ComponentPreview>

## 屬性

<PropsTable name="CodeDemo" />
