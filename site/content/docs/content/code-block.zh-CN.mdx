import { CodeBlock, InlineCode } from "yunui/content";

由 [Shiki](https://shiki.style) 驱动的语法高亮代码块。高亮器在首次渲染代码块时按需加载，因此不会打进初始包。可显示可选的 `filename` 标头、行号、高亮行、复制按钮，以及——在你传入 `onEdit` 时——回传代码载荷的「编辑」操作。把代码作为子字符串传入。

## 导入

```tsx
import { CodeBlock, InlineCode } from "@yuhuanowo/yunui/content";
```

## 示例

<ComponentPreview
  title="CodeBlock"
  code={`import { CodeBlock } from "@yuhuanowo/yunui/content";`}
>
  <div className="w-full max-w-xl text-left">
    <CodeBlock language="tsx" filename="button.tsx" showLineNumbers highlightLines={[2]}>{`export function Button({ label }: { label: string }) {
  return <button className="btn btn-primary">{label}</button>;
}`}</CodeBlock>
  </div>
</ComponentPreview>

## 行内代码

`InlineCode` 在正文中渲染单个代码标记。

<ComponentPreview
  title="InlineCode"
  code={`import { InlineCode } from "@yuhuanowo/yunui/content";`}
>
  <p className="text-left">
    Run <InlineCode>pnpm build</InlineCode> then restart the dev server.
  </p>
</ComponentPreview>

## 属性

<PropsTable
  name="CodeBlock"
  rows={[
    { name: "children", type: "string", required: true, description: "要渲染的代码（字符串形式）。" },
    { name: "language", type: "string", description: "用于高亮的语言标识（例如 tsx、python、bash）。" },
    { name: "filename", type: "string", description: "显示在代码块头部；同时会传递给 onEdit。" },
    { name: "showLineNumbers", type: "boolean", defaultValue: "false", description: "渲染行号栏。" },
    { name: "highlightLines", type: "number[]", description: "要强调的行号（从 1 开始）。" },
    { name: "onEdit", type: "(payload: CodeBlockEditPayload) => void", description: "设置后，会显示一个编辑按钮，点击时以 { code, language, filename } 调用该回调。" },
    { name: "className", type: "string", description: "包裹元素的额外类名。" },
  ]}
/>
