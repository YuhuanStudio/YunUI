import { CodeBlock, InlineCode } from "yunui/content";

由 [Shiki](https://shiki.style) 驅動的語法高亮程式碼區塊。高亮器在首次渲染程式碼區塊時按需載入，因此不會打進初始套件。可顯示可選的 `filename` 標頭、行號、高亮行、複製按鈕，以及——在你傳入 `onEdit` 時——回傳程式碼載荷的「編輯」操作。把程式碼作為子字串傳入。

## 匯入

```tsx
import { CodeBlock, InlineCode } from "@yuhuanowo/yunui/content";
```

## 範例

<ComponentPreview
  title="CodeBlock"
  code={`import { CodeBlock } from "@yuhuanowo/yunui/content";`}
>
  <div className="w-full max-w-xl text-left">
    <CodeBlock language="tsx" filename="button.tsx" showLineNumbers highlightLines={[2]}>{`export function Button({ label }: { label: string }) {
  return <button className="btn btn-primary">{label}</button>;
}`}</CodeBlock>
  </div>
</ComponentPreview>

## 行內程式碼

`InlineCode` 在內文中渲染單一程式碼標記。

<ComponentPreview
  title="InlineCode"
  code={`import { InlineCode } from "@yuhuanowo/yunui/content";`}
>
  <p className="text-left">
    Run <InlineCode>pnpm build</InlineCode> then restart the dev server.
  </p>
</ComponentPreview>

## 屬性

<PropsTable
  name="CodeBlock"
  rows={[
    { name: "children", type: "string", required: true, description: "要渲染的程式碼（字串形式）。" },
    { name: "language", type: "string", description: "用於語法高亮的語言識別碼（例如 tsx、python、bash）。" },
    { name: "filename", type: "string", description: "顯示在程式碼區塊標頭；同時會傳遞給 onEdit。" },
    { name: "showLineNumbers", type: "boolean", defaultValue: "false", description: "渲染行號欄。" },
    { name: "highlightLines", type: "number[]", description: "要強調的行號（從 1 開始）。" },
    { name: "onEdit", type: "(payload: CodeBlockEditPayload) => void", description: "設定後，會顯示一個編輯按鈕，點擊時以 { code, language, filename } 呼叫此回呼。" },
    { name: "className", type: "string", description: "外層容器的額外類名。" },
  ]}
/>
